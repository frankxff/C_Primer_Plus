#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	int num;
	num = 1;
	printf("Thia is a simple");
	printf("computer!\n"); 
	printf("The number is %d because it is first!", num);
	return 0;
	 
} 
